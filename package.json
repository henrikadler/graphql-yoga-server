{
  "name": "graphql-stitching-server",
  "version": "0.0.2",
  "description": "GraphQl Stitching Test server",
  "main": "build",
  "scripts": {
    "dev": "nodemon --watch src --exec babel-node src",
    "build": "babel src --copy-files --source-maps --out-dir build",
    "start": "node build"
  },
  "dependencies": {
    "apollo-link-context": "^1.0.8",
    "apollo-link-http": "^1.5.4",
    "graphql-tools": "^3.0.2",
    "graphql-yoga": "^1.14.7"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-module-resolver": "^3.1.1",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "eslint": "^4.19.1",
    "nodemon": "^1.17.5",
    "standard": "^11.0.1"
  }
}
